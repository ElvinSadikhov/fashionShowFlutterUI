import 'package:flutter/material.dart';

import 'package:flutter/material.dart';

const COLOR_WHITE = Colors.white;

const COLOR_BLACK = Colors.black87;

const COLOR_GREY = Colors.grey;

const COLOR_PURPLE = Color.fromRGBO(103, 65, 209, 100);

const TextTheme DEFAULT_TEXT_THEME = TextTheme(
    // for Week in Paris
    headline1: TextStyle(
        color: COLOR_PURPLE, fontWeight: FontWeight.w900, fontSize: 36),
    // 2021 Fashion show in Paris
    headline2: TextStyle(
        color: COLOR_BLACK, fontWeight: FontWeight.w500, fontSize: 13),
    // Explore
    headline3: TextStyle(
        color: COLOR_BLACK, fontWeight: FontWeight.w900, fontSize: 26),
    // scroll view
    headline4: TextStyle(
        color: COLOR_BLACK, fontWeight: FontWeight.w700, fontSize: 14),
    // -character names
    headline5: TextStyle(
        color: COLOR_BLACK, fontWeight: FontWeight.w800, fontSize: 18),
    // -city name
    headline6: TextStyle(
        color: COLOR_BLACK, fontWeight: FontWeight.w400, fontSize: 16));
